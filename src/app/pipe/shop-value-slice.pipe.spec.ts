import { ShopValueSlicePipe } from './shop-value-slice.pipe';

describe('AddressPipe', () => {
  it('create an instance', () => {
    const pipe = new ShopValueSlicePipe();
    expect(pipe).toBeTruthy();
  });
});
